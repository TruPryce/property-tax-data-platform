## 1. Ellis Mapping and Fingerprint Gate

<!-- countyforge-task: 1.1 paths=libs/property-tax-adapters/src/property_tax_adapters/sources/texas/ellis.py checks=repo.check risk=higher_risk prerequisites=D1 -->
- [x] 1.1 Declare the versioned Ellis mapping and its independent fingerprint gate — Declare the Ellis property layout using the shared `PacsField` and `PacsLayout` types from `property_tax_adapters.sources.pacs`, defining no position-slicing, layout-validation, or fingerprint logic of its own. Add the expected Ellis fingerprint constant and compare the declared mapping against it before any record is read, rejecting a mismatch with `unsupported_layout_fingerprint`. Keep the Ellis and Denton fingerprints independent, assume no equivalence from the shared vendor, and import nothing from the Denton module. Preserve `ELLIS_SOURCE` and the registry import surface.

## 2. Layout Package Classification and Scenario Labels

<!-- countyforge-task: 2.1 paths=libs/property-tax-adapters/src/property_tax_adapters/sources/texas/ellis.py checks=repo.check risk=higher_risk prerequisites=D2,D3,1.1 -->
- [x] 2.1 Classify the layout package by content and gate the release label — Add `classify_layout_package`, recognising an OpenDocument Spreadsheet package by the ZIP local-file-header signature followed by a first member named `mimetype` whose stored value is `application/vnd.oasis.opendocument.spreadsheet`. Classify from content alone so a name ending in `.xlsx.ods` is parsed as ODS, and fail closed on an absent, truncated, or ambiguous signature. Keep the check bounded: extract nothing, enumerate nothing, decompress nothing, and add no archive library. Gate the caller-supplied release label so `certified-all-property` proceeds and a potential-exemption, mineral-only, or ambiguous label is rejected with `unsupported_scenario_label` before any record is read.

## 3. Ellis Records, Grain, and Diagnostics

<!-- countyforge-task: 3.1 paths=libs/property-tax-adapters/src/property_tax_adapters/sources/texas/ellis.py,libs/property-tax-adapters/tests/fixtures/ellis_synthetic.py checks=repo.check risk=higher_risk prerequisites=D4,2.1 -->
- [x] 3.1 Implement the Ellis physical, lexical, and grain rules with independent fixtures — Implement strict ISO-8859-1 decoding with BOM rejection, LF and CRLF boundaries with one trailing ending allowed, a bare CR that is not a boundary, control characters refused as `invalid_source_text`, one-based physical row numbers with no header row, and a uniform observed width whose outliers are rejected with `record_width_mismatch`. Implement the D4 lexical grammars, empty-text-only nulls, `prop_id` compared as text, `(prop_id, owner_sequence)` owner-row grain with `duplicate_owner_row` rejection and no deduplication, summing, or arbitrary-row selection, `conflicting_account_facts` rejection, the closed eighteen-code vocabulary with exactly four permitted metadata fields, the 100-entry retention cap with preserved total and deterministic truncation, and release-level atomic rejection. Return the bounded `EllisValidationReport`; construct no record type and define no county-local replacement for a shared contract. Add `ellis_synthetic.py` with small, independently authored, identity-free members and package byte fixtures, each with literally authored expected results and no county bytes, production rows, archives, published layouts, owner values, or addresses.

## 4. Binding, Package, and Boundary Tests

<!-- countyforge-task: 4.1 paths=libs/property-tax-adapters/tests/test_ellis_parser.py,libs/property-tax-adapters/tests/fixtures/ellis_synthetic.py checks=repo.check risk=normal prerequisites=D1,D2,D3,D4,1.1,2.1,3.1 -->
- [x] 4.1 Add Ellis binding, package-classification, and boundary tests — Assert that Ellis binds to the shared component rather than forking it, defines no slicing or fingerprint logic, and imports nothing from the Denton module; that the Ellis and Denton fingerprints differ and neither is derived from the other; and that a caller-supplied Denton fingerprint is rejected. Cover ODS recognition behind a misleading `.xlsx.ods` name, fail-closed behaviour on absent, truncated, and ambiguous signatures, and that no member is extracted or decompressed. Cover certified-label acceptance and rejection of potential-exemption, mineral-only, and ambiguous labels before any record is read. Cover the physical layer, every lexical boundary, text-compared account identity, owner allocations accepted, duplicate owner rows and conflicting account facts rejected, the closed vocabulary and its four permitted fields, deterministic truncation, atomic rejection, and exclusion of owner, address, and trailing-region values. For architecture, assert that `property_tax_domain` and `property_tax_application` are unchanged and gain no PACS or Ellis parser vocabulary — the existing `ELLIS_SOURCE` legitimately imports both — that no county-local `SourceNativeValue`, `SourceProvenance`, or `AppraisalSourceRecord` is defined, and that no network, archive, persistence, or new-dependency behavior is present. Ensure `make test` collects this exact module.

## 5. Bounded Source Documentation

<!-- countyforge-task: 5.1 paths=docs/sources/ellis-pacs-parser-binding.md checks=docs.links,repo.prepr-no-ai risk=low prerequisites=4.1 -->
- [x] 5.1 Document the Ellis binding — Record that Ellis binds to the shared PACS component rather than forking it, the independent fingerprint gate and why vendor equivalence is not evidence, the content-based package classification and its fail-closed behaviour, the approved certified label and the excluded scenario labels, the physical and lexical rules, the owner-row grain and the absence of any account roll-up, the closed diagnostic vocabulary, the caller-supplied release identity, the privacy default-deny posture, and the synthetic fixture provenance. State explicitly that live-release compatibility, the published Ellis layout, the observed widths, whether Ellis and Denton layouts genuinely agree, source licensing, and production readiness remain unproved, that discovery, rendered-page retrieval, archive and ODS extraction, historical release modelling, the evidence manifest, and account roll-up are outside this change, and that typed record output is deferred to Issue #43.
