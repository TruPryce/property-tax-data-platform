## 1. Reusable PACS Serialization Component

<!-- countyforge-task: 1.1 paths=libs/property-tax-adapters/src/property_tax_adapters/sources/pacs.py checks=repo.check risk=higher_risk prerequisites=D1,D2 -->
- [x] 1.1 Add the shared fixed-width serialization component — Add `property_tax_adapters/sources/pacs.py` with `PacsField` (name, 1-indexed inclusive `start` and `end`, required flag), `PacsLayout` (layout identifier, layout version, ordered fields) validating ascending order, non-overlap, and declared-length agreement at construction and raising `ValueError` for those authoring defects, `layout_fingerprint` over the exact five-key canonical document from D2, and `slice_record` performing 1-indexed inclusive slicing that never emits a partial value, reports a required field ending beyond the observed width, and returns a structural fingerprint of any undocumented trailing region. The module carries serialization mechanics only: no county name, no county field, no threshold, and no policy, so Ellis can bind to it rather than fork it. Add no network, archive, or dependency behavior.

## 2. Denton Layout and Physical Validation

<!-- countyforge-task: 2.1 paths=libs/property-tax-adapters/src/property_tax_adapters/sources/texas/denton.py,libs/property-tax-adapters/tests/fixtures/denton_synthetic.py checks=repo.check risk=higher_risk prerequisites=D1,D2,D3,1.1 -->
- [x] 2.1 Add the versioned Denton layout, physical decoding, and independent fixtures — Declare the versioned Denton property and child layouts using the shared component. Implement strict ISO-8859-1 decoding with BOM rejection, LF and CRLF boundaries with one trailing ending allowed, a bare CR that is not a boundary, one-based physical row numbers with no header row, and expected-width validation rejecting `record_width_mismatch`. Add `denton_synthetic.py` with small, independently authored, identity-free synthetic members covering valid records, width mismatch, truncated required fields, trailing regions, lexical boundaries, owner allocations, duplicate owner rows, conflicting account facts, and child orphans, each with literally authored expected results and no mirrored parser, county bytes, production rows, owner values, addresses, layouts, or archives. Preserve `DENTON_SOURCE` and the registry import surface.

## 3. Denton Lexical, Grain, and Relationship Rules

<!-- countyforge-task: 3.1 paths=libs/property-tax-adapters/src/property_tax_adapters/sources/texas/denton.py checks=repo.check risk=higher_risk prerequisites=D3,D4,2.1 -->
- [x] 3.1 Implement the Denton lexical grammars, owner grain, and child classification — Implement the D3 grammars for `prop_id`, `owner_sequence`, monetary values, `ownership_percentage`, and the tax year matched against the caller-supplied expected year; empty-text-only nulls; `prop_id` compared as text so `000123` and `123` are distinct; `(prop_id, owner_sequence)` owner-row grain with `duplicate_owner_row` rejection and no deduplication, summing, or arbitrary-row selection; `conflicting_account_facts` rejection when records sharing `prop_id` disagree on a declared account-level fact; `ten_percent_cap` preserved as a source-native cap amount with no canonical value derived from it; and D4 child classification where a core appraisal orphan blocks and a legal orphan warns. Implement the closed diagnostic vocabulary with exactly four permitted metadata fields, the 100-entry retention cap with preserved total and deterministic truncation, and release-level atomic rejection returning zero accepted rows. Return the bounded `DentonValidationReport`; construct no record type and import nothing from a shared contracts module.

## 4. Contract, Privacy, and Boundary Tests

<!-- countyforge-task: 4.1 paths=libs/property-tax-adapters/tests/test_denton_parser.py,libs/property-tax-adapters/tests/fixtures/denton_synthetic.py checks=repo.check risk=normal prerequisites=D1,D2,D3,D4,1.1,2.1,3.1 -->
- [x] 4.1 Add PACS component and Denton parser tests — Cover component position slicing, construction-time rejection of unordered, overlapping, and length-inconsistent layouts, fingerprint stability across processes and its sensitivity to every declared attribute, refusal to emit a truncated required field, and trailing-region fingerprinting. Cover Denton encoding and BOM rejection, LF and CRLF, bare CR, width mismatch with its physical row number, every lexical accept and reject boundary, empty-text-only nulls, text-compared account identity, owner allocations accepted, duplicate owner rows and conflicting account facts rejected, `ten_percent_cap` carried without interpretation, core orphans blocking and legal orphans warning, the closed vocabulary and its four permitted fields, deterministic truncation with preserved total, atomic rejection, and exclusion of owner, address, and trailing-region values from the report and diagnostics. For architecture, assert that `property_tax_domain` and `property_tax_application` are unchanged and gain no PACS or Denton parser vocabulary — the existing `DENTON_SOURCE` legitimately imports both, so absence of those imports is not the assertion — that no county-local `SourceNativeValue`, `SourceProvenance`, or `AppraisalSourceRecord` is defined, and that no network, archive, persistence, or new-dependency behavior is present. Ensure `make test` collects this exact module.

## 5. Bounded Source Documentation

<!-- countyforge-task: 5.1 paths=docs/sources/denton-pacs-parser-foundation.md checks=docs.links,repo.prepr-no-ai risk=low prerequisites=4.1 -->
- [x] 5.1 Document the shared PACS component and the Denton binding — Record the split between reusable serialization mechanics and county policy, the 1-indexed inclusive position rules, the exact fingerprint document, the physical and lexical rules, the owner-row grain and the absence of any account roll-up, the `ten_percent_cap` treatment, the child classification, the closed diagnostic vocabulary, the caller-supplied release identity, the privacy default-deny posture, and the synthetic fixture provenance. State explicitly that live-release compatibility, the published layout, the observed widths, `ten_percent_cap` semantics, orphan rates, source licensing, and production readiness remain unproved, that discovery, archives, roll precedence, the evidence manifest, and account roll-up are outside this change, and that typed record output is deferred to Issue #43.

## 6. Typed Records and Shared Conversion — Blocked on Issue #43

The three slices below construct or consume contracts owned by Issue #43 and
are runnable only after those contracts are accepted and implemented. Tasks 1.1
through 5.1 do not depend on them. Issue #20 is not complete until these land.

<!-- countyforge-task: 6.1 paths=libs/property-tax-adapters/src/property_tax_adapters/sources/texas/denton.py,libs/property-tax-adapters/tests/test_denton_parser.py checks=repo.check risk=higher_risk prerequisites=D3,3.1,4.1 -->
- [ ] 6.1 Construct the typed Denton source record and its provenance — **Blocked on Issue #43.** Define the frozen Denton source record carrying `prop_id`, `owner_sequence`, the approved owner-scoped and account-level values as shared `SourceNativeValue` entries, `ten_percent_cap` as a source-native cap amount, and a Denton provenance carrying jurisdiction, release identifier, source member name, tax year, layout fingerprint and version, field positions, and the one-based physical row number. Add a row-materialization entry point that reuses the existing validation rather than reimplementing it, and keep `DentonValidationReport` valid and record-free. Create no county-local substitute for `SourceNativeValue`, `SourceProvenance`, or `AppraisalSourceRecord` while waiting.

<!-- countyforge-task: 6.2 paths=libs/property-tax-adapters/src/property_tax_adapters/sources/texas/denton.py,libs/property-tax-adapters/tests/test_denton_parser.py checks=repo.check risk=higher_risk prerequisites=D3,6.1 -->
- [ ] 6.2 Convert Denton records through the shared Issue #43 contracts — **Blocked on Issue #43.** Convert each valid Denton record into the shared `AppraisalSourceRecord` with jurisdiction `tx-denton`, `prop_id` as source account ID, source-native values keyed by exact Denton field name, and shared provenance derived from the Denton provenance. Preserve `(prop_id, owner_sequence)` grain in the output, derive no account roll-up, and populate no canonical market, appraised, assessed, taxable, tax-amount, exemption-entitlement, or capped-value field. Add conversion tests to the existing Denton test module.

<!-- countyforge-task: 6.3 paths=libs/property-tax-adapters/src/property_tax_adapters/sources/texas/denton.py,libs/property-tax-adapters/tests/test_denton_parser.py checks=repo.check risk=higher_risk prerequisites=D3,D5,6.1 -->
- [ ] 6.3 Construct typed Denton child records and their provenance — **Blocked on Issue #43.** Define the typed child record carrying `prop_id`, `child_sequence`, the child value as a shared `SourceNativeValue`, and a child provenance carrying the child table, layout fingerprint and version, and the one-based physical row number. Apply the D5 child lexical bounds: `child_sequence` one to four ASCII digits and required, `child_value` the property monetary grammar bounded 0 through `10**26 - 1` and optional, with empty text after trimming as the only null. Those bounds are decided in this plan rather than chosen during implementation. Preserve each child at its measured source grain, derive no roll-up to the parent account, and keep the core-blocking and legal-warning classification intact.
