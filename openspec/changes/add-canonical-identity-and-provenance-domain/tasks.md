# Tasks

## 1. Domain identity values

- [ ] 1.1 Add the jurisdiction value object — Add `libs/property-tax-domain/src/property_tax_domain/jurisdiction.py` defining `Jurisdiction` as a frozen slotted value object whose identity is `state_code` and `county_slug` and whose `county_fips` is required validated registry metadata excluded from equality and hashing. Validate `state_code` as exactly two lowercase ASCII letters, `county_slug` as `[a-z0-9]+(-[a-z0-9]+)*`, and `county_fips` as exactly five digits, rejecting an uppercase or mixed-case component rather than normalizing it. Case-fold on the registry side when comparing against `County.state_code`, which is stored as `TX`, so the uppercase datum stays valid where it lives and no caller-supplied identity is rewritten. Reject registry data that contradicts identity by checking the supplied slug and FIPS against the version-controlled registry, so the Collin slug paired with Dallas County's FIPS fails at construction. Expose the canonical `tx-collin` rendering. Keep `county_fips` out of equality, out of hashing, and out of the jurisdiction identity document; it serializes as registry metadata keyed by the identity. Keep `counties.py` as the registry and do not duplicate its data. Import only the standard library.
<!-- countyforge-task: 1.1 paths=libs/property-tax-domain/src/property_tax_domain/jurisdiction.py checks=repo.check risk=higher_risk prerequisites=D1 -->

- [ ] 1.2 Add artifact content identity — Add `libs/property-tax-domain/src/property_tax_domain/artifact.py` defining `ArtifactIdentity` carrying a SHA-256 digest and nothing else. Reject any digest that is not exactly sixty-four lowercase hexadecimal characters rather than trimming, padding, or case-folding it. Declare `ARTIFACT_IDENTITY_HEX_LENGTH: Final = 64` in this module and validate the digest against it rather than against a repeated literal, so the constant task 2.2 exports is defined by the task that owns the file. Provide the bare 64-character rendering. Add no object-store URI, bucket, key, URL, filename, entity tag, surrogate identifier, or timestamp field.
<!-- countyforge-task: 1.2 paths=libs/property-tax-domain/src/property_tax_domain/artifact.py checks=repo.check risk=normal prerequisites=D4 -->

- [ ] 1.3 Add the canonical release kind vocabulary — Add `libs/property-tax-domain/src/property_tax_domain/release_kind.py` defining `ReleaseKind` as a closed `StrEnum` of exactly `proposed`, `certified`, `supplemental`, and `current`. Provide no open-string constructor and no fallback member. Declare no county name and no county-native label. Runtime mapping from a native label is **not added by this change**: the capability spec holds the mapping table and task 3.1 only mirrors it into engineering documentation, and the code boundary is deferred under D9 to bootstrap task 2.4, which needs its own scope decision. That deferral deliberately creates no task here, because the implementation parser has no blocked state and would dispatch one as ordinary work.
<!-- countyforge-task: 1.3 paths=libs/property-tax-domain/src/property_tax_domain/release_kind.py checks=repo.check risk=normal prerequisites=D3 -->

- [ ] 1.4 Add logical release identity — Add `libs/property-tax-domain/src/property_tax_domain/release.py` defining `ReleaseIdentity` carrying `Jurisdiction`, `tax_year`, `ReleaseKind`, and a required `release_identifier`. Bound the identifier to 1–128 characters of `[A-Za-z0-9._-]` not beginning with `.` or `-`, matching the alphabet already accepted across the repository, and reject anything outside it rather than trimming, lowercasing, or slugifying it. Preserve case and treat it as significant: `ABC` and `abc` are both valid and denote different releases, because both letter cases are inside the alphabet. Reject surrounding whitespace because whitespace is outside the grammar, which is a different rule from case. Bound the tax year to 1900 through 2200 inclusive, matching `ReleasePartition` and the merged `partition_tax_year_plausible` constraint, and reject `bool`. Do not adopt the narrower 1900–2100 parser bound, which applies to a source year rather than to a release identity. Carry no artifact identity and no acquisition timestamp.
<!-- countyforge-task: 1.4 paths=libs/property-tax-domain/src/property_tax_domain/release.py checks=repo.check risk=higher_risk prerequisites=D1,D2,D3,1.1,1.3 -->

- [ ] 1.5 Add the artifact-release association — Add `ArtifactReleaseBinding` to `libs/property-tax-domain/src/property_tax_domain/release.py` as an immutable pairing of one `ArtifactIdentity` and one `ReleaseIdentity`. Store no collection on either identity, so that binding an additional artifact to a release cannot change that release's identity and one artifact can back several releases.
<!-- countyforge-task: 1.5 paths=libs/property-tax-domain/src/property_tax_domain/release.py checks=repo.check risk=normal prerequisites=D4,1.2,1.4 -->

- [ ] 1.6 Add bounded domain provenance — Add `libs/property-tax-domain/src/property_tax_domain/provenance.py` defining `DomainProvenance` composing `ReleaseIdentity` and `ArtifactIdentity`, with exactly these remaining fields and no others: `source_member_name: str` bounded to 1–128 characters of `[A-Za-z0-9._-]` not beginning with `.` or `-`; `source_row_number: int | None` one-based, rejecting `bool` and any value below 1, `None` only where the source has no row grain; `parser_contract_version: int` required and at least 1, rejecting `bool`; `layout_fingerprint: str | None` of exactly 64 lowercase hexadecimal characters matching the SHA-256 hexdigest the county adapters already produce, `None` only where the source has no layout. Reject anything outside those types, bounds, or alphabets rather than truncating, padding, or case-folding. Do not restate jurisdiction, tax year, release kind, or release identifier as independent fields. Add no generic payload, detail, extra, metadata, or annotation field, no mapping, and no sequence of arbitrary values. Represent absence as `None` and never as an empty string, a zero row number, or a zero-filled fingerprint.
<!-- countyforge-task: 1.6 paths=libs/property-tax-domain/src/property_tax_domain/provenance.py checks=repo.check risk=higher_risk prerequisites=D5,1.2,1.4 -->

## 2. Serialization

- [ ] 2.1 Add named-field JSON serialization and the derived compact renderings — Add `libs/property-tax-domain/src/property_tax_domain/serialization.py` implementing the six named-field shapes the capability spec fixes: jurisdiction identity, artifact, release, binding, provenance, and the separate registry-metadata document. Nest identity values as objects rather than pre-rendered strings; emit an absent optional as JSON `null` rather than omitting the key, so absence is distinguishable from an older schema; fix key order to the declared order rather than insertion or hash order; and keep `county_fips` out of the jurisdiction identity document, serializing registry metadata as its own document keyed by the identity. Resolve `county_fips` from the version-controlled registry when parsing a jurisdiction identity document, failing when the registry describes no such slug rather than inventing or retaining a stale value; give the registry document its own parser that rejects a FIPS disagreeing with the registry and never reconstructs identity in its place. Derive the compact renderings — `tx-collin`, the bare digest, and `tx-collin/2025/certified/<release_identifier>` — and provide a reverse parser that splits into exactly four components. Do not treat a compact rendering as the contract and do not derive the JSON from it.
<!-- countyforge-task: 2.1 paths=libs/property-tax-domain/src/property_tax_domain/serialization.py checks=repo.check risk=higher_risk prerequisites=D6,1.1,1.2,1.4,1.6 -->

- [ ] 2.2 Declare the public domain surface — Set `__all__` in `libs/property-tax-domain/src/property_tax_domain/__init__.py` to exactly `ARTIFACT_IDENTITY_HEX_LENGTH`, `ArtifactIdentity`, `ArtifactReleaseBinding`, `County`, `CountySlug`, `DomainProvenance`, `INITIAL_COUNTIES`, `Jurisdiction`, `ReleaseIdentity`, `ReleaseKind`, `county_by_slug`. Retain the four existing names: `CountySlug` and `county_by_slug` are imported from the package root by seven county adapter modules and removing them breaks each. Do not export serialization functions at the root; they stay reachable through the serialization module. Add no re-export of a private helper and no wildcard.
<!-- countyforge-task: 2.2 paths=libs/property-tax-domain/src/property_tax_domain/__init__.py checks=repo.check risk=normal prerequisites=D8,1.1,1.2,1.3,1.4,1.5,1.6 -->

## 3. Adapter-boundary mapping, without rewriting a layer

- [ ] 3.1 Mirror the normative mapping table into engineering documentation — The capability spec holds the mapping table as normative data, because the promoted spec is what survives archival as contract; this task **mirrors** it into `docs/engineering/canonical-identity.md` for readers and does not create it. Reproduce the supported rows — Dallas `proposed`, `certified`, and `certified-with-supplemental`; Collin `current` and `certified`; Denton, Ellis, and Tarrant `certified` — and the unmapped Denton `preliminary` and `roll-correction` rows with their reasons, and state that the capability spec governs if the two ever disagree. Write **no mapping code in this change**. The shared `property_tax_adapters.sources.contracts` module is county-neutral by its accepted contract, and its own suite asserts that no county name appears in it and that it imports neither `property_tax_domain` nor `property_tax_application`; adding a county-to-`ReleaseKind` table there would fail those tests, which this change does not authorize changing. Record the deferral itself under D9: code mapping belongs to the adapter/application integration task that owns a county-aware boundary, bootstrap task 2.4, and a separate scope decision must authorize where it lives.
<!-- countyforge-task: 3.1 paths=docs/engineering/canonical-identity.md checks=repo.check risk=normal prerequisites=D3,D7,D9,1.3 -->

- [ ] 3.2 Document the cross-layer mapping — Update `libs/property-tax-domain/README.md` and add `docs/engineering/canonical-identity.md` recording how `ReleasePartition`, `StoredArtifact.sha256`, `bronze.release_partition`, `SourceProvenance`, the `silver.source_record` provenance columns, and the S3 `artifact_key` and `partition_prefix` renderings map onto these types, and which task performs each mapping. State that no layer is rewritten by this change. Add the documents to link checking.
<!-- countyforge-task: 3.2 paths=libs/property-tax-domain/README.md,docs/engineering/canonical-identity.md checks=repo.check risk=normal prerequisites=1.1,1.2,1.4,1.6 -->

## 4. Falsification tests

- [ ] 4.1 Prove identity rules by attacking them — Add `libs/property-tax-domain/tests/test_identity.py` asserting behavior rather than restating types. Prove that `tx-dallas` and `tx-collin` carrying the same source release identifier, tax year, and kind are unequal; that identities differing only in release identifier are unequal; that two different digests are different artifacts under one source URL and one filename, and that one digest under two URLs and two filenames is one artifact; that a slug paired with another county's FIPS is rejected; that a FIPS that is not five digits is rejected; that a digest of the wrong length, wrong case, or wrong alphabet is rejected rather than coerced; that an out-of-range tax year, a `bool` tax year, a kind outside the closed vocabulary, and an identifier outside the accepted alphabet are each rejected rather than coerced; that an identifier carrying surrounding whitespace or any character outside the alphabet is rejected rather than trimmed or rewritten; and that two identifiers differing only by case are **both accepted** and denote unequal release identities, since rejecting one would invalidate half the accepted alphabet. Assert `ARTIFACT_IDENTITY_HEX_LENGTH == 64` and that it is the same bound construction enforces, so the published constant and the validation cannot drift apart. Assert that no object-store location, URL, filename, entity tag, surrogate key, orchestration run identifier, or timestamp participates in any equality or hash.
<!-- countyforge-task: 4.1 paths=libs/property-tax-domain/tests/test_identity.py checks=repo.check risk=higher_risk prerequisites=1.1,1.2,1.3,1.4 -->

- [ ] 4.2 Prove the association and the blocked-discrimination boundary — Add `libs/property-tax-domain/tests/test_binding.py` proving one artifact binds to several release identities without duplicating or mutating the artifact identity, and that a release observed in a second artifact keeps an unchanged release identity — the Bronze divergence case, where the same identity arrives with a different checksum and both are kept. Prove that the domain constructor rejects a missing or malformed release identifier, and prove by assertion on the boundary that it does **not** attempt to judge whether a syntactically valid identifier was approved, since that is a fact about a county contract the domain does not import. Record that the Tarrant refusal is tested at the county mapping boundary by the later integration task, not here.
<!-- countyforge-task: 4.2 paths=libs/property-tax-domain/tests/test_binding.py checks=repo.check risk=higher_risk prerequisites=D2,1.4,1.5 -->

- [ ] 4.3 Prove serialization determinism and reversibility — Add `libs/property-tax-domain/tests/test_serialization.py` proving equal values serialize byte-identically including field order; that every value other than a jurisdiction identity document round-trips unaltered; that a jurisdiction identity document round-trips its two identity components and compares equal, while its FIPS comes from the registry rather than from the document, so a document written before a registry correction parses with the corrected value rather than detecting the change; that the registry document parser returns the identity together with the FIPS the document recorded, rejects a FIPS disagreeing with the registry, and rejects a slug the registry does not describe; and that the compact release rendering is reversible for every accepted release identifier. Derive the identifier cases from the accepted alphabet itself rather than a fixed list, covering every single admissible character plus the length boundaries and adversarial forms, so widening the alphabet to admit the separator fails this test rather than making the rendering ambiguous. Assert that no compact rendering of two distinct identities collides.
<!-- countyforge-task: 4.3 paths=libs/property-tax-domain/tests/test_serialization.py checks=repo.check risk=higher_risk prerequisites=D6,2.1 -->

- [ ] 4.4 Prove the provenance boundary holds — Add `libs/property-tax-domain/tests/test_provenance.py` proving the enforceable property rather than an overclaim: domain provenance exposes exactly its specified fields and no other, none of them a mapping, a sequence of arbitrary values, or a generic payload, detail, extra, metadata, or annotation field. Do not assert that no field can receive identifying data — a bounded `source_member_name` can still be handed `JOHN_DOE`, and a test claiming otherwise would be false. Prove each declared bound at its boundary: an over-length or out-of-alphabet member name, a zero or negative or `bool` row number, a parser version below 1, and a fingerprint of the wrong length, case, or alphabet are each rejected rather than coerced. Prove jurisdiction, tax year, release kind, and release identifier are reachable only through the composed release identity rather than restated, and that absence is `None` rather than an empty string, a zero row number, or a zero-filled fingerprint.
<!-- countyforge-task: 4.4 paths=libs/property-tax-domain/tests/test_provenance.py checks=repo.check risk=higher_risk prerequisites=D5,1.6 -->

- [ ] 4.5 Prove the domain stays independent — Extend `tests/architecture/test_dependency_direction.py` to assert that `property_tax_domain` imports no adapter, application, object-store, database, orchestration, or county-specific module, naming boto3, psycopg, and Airflow explicitly, and that the domain's identity and provenance types are constructible with those packages absent from the import graph. Assert that adapter-only vocabulary — table name, source family, source status, observed-field and normalized-field vectors, and county-native field names — appears nowhere in `property_tax_domain`.
<!-- countyforge-task: 4.5 paths=tests/architecture/test_dependency_direction.py checks=repo.check risk=higher_risk prerequisites=1.1,1.2,1.3,1.4,1.6 -->

- [ ] 4.6 Prove the public surface is deliberate — Add `libs/property-tax-domain/tests/test_public_surface.py` asserting the exported set of `property_tax_domain` equals the eleven names the capability enumerates, written out in the test rather than read from the implementation, so that both adding a name and removing one of the four pre-existing names fail. Assert each type is reachable from the package root without consumers directly importing submodules; the package root necessarily imports them internally, so do not assert that no submodule import occurs.
<!-- countyforge-task: 4.6 paths=libs/property-tax-domain/tests/test_public_surface.py checks=repo.check risk=normal prerequisites=D8,2.2 -->

- [ ] 4.7 Prove existing layers are untouched — Assert that the acquisition, Bronze, bounded-release-processing, county-adapter, and migration suites remain green and that this change alters none of their behavior. Confirm no PostgreSQL migration, S3 key, orchestration, or API surface changed.
<!-- countyforge-task: 4.7 paths=tests/architecture/test_dependency_direction.py checks=repo.check risk=normal prerequisites=4.1,4.2,4.3,4.4,4.5,4.6 -->

<!-- Section 5 is intentionally absent. It held a task marked BLOCKED in prose for
     deferred runtime label mapping. The implementation parser has no blocked state —
     measured, it reports every unchecked task as `incomplete` — so that task would
     have been dispatched as ordinary work with only a documentation path writable.
     The deferral is recorded in D9, which names bootstrap task 2.4 as owner and
     creates no task. Numbering is left as-is so this and review 5001441740 refer to
     the same task ids. -->

## 6. Completion

- [ ] 6.1 Run the full no-cost gates and record the outcome — Run `openspec validate --all --strict`, `openspec doctor`, documentation-link validation, `make check`, and `make prepr-no-ai`, and record the validated state in `docs/engineering/canonical-identity.md` together with the deferred runtime mapping and its owner. Do **not** update `openspec/changes/bootstrap-six-county-appraisal-platform/tasks.md`: `openspec` is a prohibited write root for the implementation lane, so checking bootstrap task 2.1 is not work this lane can perform. Nor can this lane check this change's own eighteen boxes: artifact validation holds an accepted change's task file immutable and raises `openspec_tasks_mutation`. Both therefore belong to the separate post-implementation completion and archival pull request, which performs, in order: reconcile and check this change's verified tasks; check bootstrap task 2.1, leaving bootstrap tasks 2.2 and 3.4 unchecked; then archive this change and promote the capability. Without that first step the change stays unarchivable with every box unchecked.
<!-- countyforge-task: 6.1 paths=docs/engineering/canonical-identity.md checks=docs.links,repo.prepr-no-ai risk=normal prerequisites=3.1,3.2,4.1,4.2,4.3,4.4,4.5,4.6,4.7 -->
