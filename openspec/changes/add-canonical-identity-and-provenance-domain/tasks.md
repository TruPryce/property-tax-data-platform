# Tasks

## 1. Domain identity values

- [ ] 1.1 Add the jurisdiction value object — Add `libs/property-tax-domain/src/property_tax_domain/jurisdiction.py` defining `Jurisdiction` as a frozen slotted value object whose identity is `state_code` and `county_slug` and whose `county_fips` is required validated registry metadata excluded from equality and hashing. Validate the state code, the slug against the accepted `^[a-z]{2}-[a-z0-9]+(-[a-z0-9]+)*$` composition, and the FIPS as exactly five digits. Reject registry data that contradicts identity by checking the supplied slug and FIPS against the version-controlled registry, so the Collin slug paired with Dallas County's FIPS fails at construction. Expose the canonical `tx-collin` rendering. Keep `counties.py` as the registry and do not duplicate its data. Import only the standard library.
<!-- countyforge-task: 1.1 paths=libs/property-tax-domain/src/property_tax_domain/jurisdiction.py checks=repo.check risk=higher_risk prerequisites=D1 -->

- [ ] 1.2 Add artifact content identity — Add `libs/property-tax-domain/src/property_tax_domain/artifact.py` defining `ArtifactIdentity` carrying a SHA-256 digest and nothing else. Reject any digest that is not exactly sixty-four lowercase hexadecimal characters rather than trimming, padding, or case-folding it. Provide the bare 64-character rendering. Add no object-store URI, bucket, key, URL, filename, entity tag, surrogate identifier, or timestamp field.
<!-- countyforge-task: 1.2 paths=libs/property-tax-domain/src/property_tax_domain/artifact.py checks=repo.check risk=normal prerequisites=D4 -->

- [ ] 1.3 Add the canonical release kind vocabulary — Add `libs/property-tax-domain/src/property_tax_domain/release_kind.py` defining `ReleaseKind` as a closed `StrEnum` of exactly `proposed`, `certified`, `supplemental`, and `current`. Provide no open-string constructor and no fallback member. Declare no county name and no county-native label; the mapping from a native label lives at the adapter boundary and is added by task 3.1.
<!-- countyforge-task: 1.3 paths=libs/property-tax-domain/src/property_tax_domain/release_kind.py checks=repo.check risk=normal prerequisites=D3 -->

- [ ] 1.4 Add logical release identity — Add `libs/property-tax-domain/src/property_tax_domain/release.py` defining `ReleaseIdentity` carrying `Jurisdiction`, `tax_year`, `ReleaseKind`, and a required `release_identifier`. Bound the identifier to 1–128 characters of `[A-Za-z0-9._-]` not beginning with `.` or `-`, matching the alphabet already accepted across the repository, and reject anything outside it rather than lowercasing, trimming, or slugifying it. Bound the tax year to the accepted range and reject `bool`. Carry no artifact identity and no acquisition timestamp.
<!-- countyforge-task: 1.4 paths=libs/property-tax-domain/src/property_tax_domain/release.py checks=repo.check risk=higher_risk prerequisites=D1,D2,D3,1.1,1.3 -->

- [ ] 1.5 Add the artifact-release association — Add `ArtifactReleaseBinding` to `libs/property-tax-domain/src/property_tax_domain/release.py` as an immutable pairing of one `ArtifactIdentity` and one `ReleaseIdentity`. Store no collection on either identity, so that binding an additional artifact to a release cannot change that release's identity and one artifact can back several releases.
<!-- countyforge-task: 1.5 paths=libs/property-tax-domain/src/property_tax_domain/release.py checks=repo.check risk=normal prerequisites=D4,1.2,1.4 -->

- [ ] 1.6 Add bounded domain provenance — Add `libs/property-tax-domain/src/property_tax_domain/provenance.py` defining `DomainProvenance` composing `ReleaseIdentity` and `ArtifactIdentity` and carrying `source_member_name`, `source_row_number` where applicable, `parser_contract_version`, and a layout fingerprint where applicable, each bounded and validated. Do not restate jurisdiction, tax year, release kind, or release identifier as independent fields. Add no field capable of holding a source row, an arbitrary source value, owner information, an address, a credential, a signed URL, a host-local path, or exception text, and no mapping, sequence, or free-form attribute of any kind. Represent absence as `None` and never as a placeholder string.
<!-- countyforge-task: 1.6 paths=libs/property-tax-domain/src/property_tax_domain/provenance.py checks=repo.check risk=higher_risk prerequisites=D5,1.2,1.4 -->

## 2. Serialization

- [ ] 2.1 Add named-field JSON serialization and the derived compact renderings — Add `libs/property-tax-domain/src/property_tax_domain/serialization.py` producing named-field JSON as the authoritative form, with deterministic field order so equal values serialize byte-identically, and parsers that round-trip without altering a component. Derive the compact renderings — `tx-collin` for a jurisdiction, the bare digest for an artifact, and `tx-collin/2025/certified/<release_identifier>` for a release — and provide a reverse parser that splits into exactly four components. Do not treat a compact rendering as the contract and do not derive the JSON from it.
<!-- countyforge-task: 2.1 paths=libs/property-tax-domain/src/property_tax_domain/serialization.py checks=repo.check risk=higher_risk prerequisites=D6,1.1,1.2,1.4,1.6 -->

## 3. Adapter-boundary mapping, without rewriting a layer

- [ ] 3.1 Add the contract-supported county label mappings — Add the native-label to canonical-kind mapping at the adapter boundary in `libs/property-tax-adapters/src/property_tax_adapters/sources/contracts.py`, covering only mappings an accepted contract supports: Dallas `proposed` and `certified`, Collin `current` and `certified`, Ellis `certified`, Tarrant `certified`, Denton `certified`. Retain the source-native label alongside the canonical kind. Refuse Denton `preliminary` and Denton `roll-correction` rather than selecting the nearest-looking canonical kind, and refuse Dallas `certified-with-supplemental` unless decision D7 is accepted by merging this change. Change no existing adapter behavior, no parser, and no county record shape.
<!-- countyforge-task: 3.1 paths=libs/property-tax-adapters/src/property_tax_adapters/sources/contracts.py checks=repo.check risk=higher_risk prerequisites=D3,D7,1.3 -->

- [ ] 3.2 Document the cross-layer mapping — Update `libs/property-tax-domain/README.md` and add `docs/engineering/canonical-identity.md` recording how `ReleasePartition`, `StoredArtifact.sha256`, `bronze.release_partition`, `SourceProvenance`, the `silver.source_record` provenance columns, and the S3 `artifact_key` and `partition_prefix` renderings map onto these types, and which task performs each mapping. State that no layer is rewritten by this change. Add the documents to link checking.
<!-- countyforge-task: 3.2 paths=libs/property-tax-domain/README.md,docs/engineering/canonical-identity.md checks=repo.check risk=normal prerequisites=1.1,1.2,1.4,1.6 -->

## 4. Falsification tests

- [ ] 4.1 Prove identity rules by attacking them — Add `libs/property-tax-domain/tests/test_identity.py` asserting behavior rather than restating types. Prove that `tx-dallas` and `tx-collin` carrying the same source release identifier, tax year, and kind are unequal; that identities differing only in release identifier are unequal; that two different digests are different artifacts under one source URL and one filename, and that one digest under two URLs and two filenames is one artifact; that a slug paired with another county's FIPS is rejected; that a FIPS that is not five digits is rejected; that a digest of the wrong length, wrong case, or wrong alphabet is rejected rather than coerced; that an out-of-range tax year, a `bool` tax year, a kind outside the closed vocabulary, and an identifier outside the accepted alphabet are each rejected rather than coerced; and that an identifier differing only by case or surrounding whitespace is rejected rather than normalized. Assert that no object-store location, URL, filename, entity tag, surrogate key, orchestration run identifier, or timestamp participates in any equality or hash.
<!-- countyforge-task: 4.1 paths=libs/property-tax-domain/tests/test_identity.py checks=repo.check risk=higher_risk prerequisites=1.1,1.2,1.3,1.4 -->

- [ ] 4.2 Prove the association and blocked discrimination — Add `libs/property-tax-domain/tests/test_binding.py` proving one artifact binds to several release identities without duplicating or mutating the artifact identity, and that a release observed in a second artifact keeps an unchanged release identity. Prove that a Tarrant mutable same-year snapshot with no approved discriminator cannot be coerced into a canonical release identity: it fails construction rather than reusing an existing identity, inventing a release kind, or using the artifact digest as the discriminator.
<!-- countyforge-task: 4.2 paths=libs/property-tax-domain/tests/test_binding.py checks=repo.check risk=higher_risk prerequisites=D2,1.4,1.5 -->

- [ ] 4.3 Prove serialization determinism and reversibility — Add `libs/property-tax-domain/tests/test_serialization.py` proving equal values serialize byte-identically including field order, that every value round-trips unaltered, and that the compact release rendering is reversible for every accepted release identifier. Derive the identifier cases from the accepted alphabet itself rather than a fixed list, covering every single admissible character plus the length boundaries and adversarial forms, so widening the alphabet to admit the separator fails this test rather than making the rendering ambiguous. Assert that no compact rendering of two distinct identities collides.
<!-- countyforge-task: 4.3 paths=libs/property-tax-domain/tests/test_serialization.py checks=repo.check risk=higher_risk prerequisites=D6,2.1 -->

- [ ] 4.4 Prove the provenance boundary holds — Add `libs/property-tax-domain/tests/test_provenance.py` proving domain provenance exposes exactly its specified fields and no other, so there is no attribute capable of receiving a source row, an arbitrary source value, owner information, an address, a credential, a signed URL, a host-local path, or exception text. Prove no field is a mapping, a sequence of arbitrary values, or otherwise free-form; that jurisdiction, tax year, release kind, and release identifier are reachable only through the composed release identity rather than restated; that an unbounded or out-of-alphabet lineage value is rejected rather than truncated; and that absence is `None` rather than a placeholder.
<!-- countyforge-task: 4.4 paths=libs/property-tax-domain/tests/test_provenance.py checks=repo.check risk=higher_risk prerequisites=D5,1.6 -->

- [ ] 4.5 Prove the domain stays independent — Extend `tests/architecture/test_dependency_direction.py` to assert that `property_tax_domain` imports no adapter, application, object-store, database, orchestration, or county-specific module, naming boto3, psycopg, and Airflow explicitly, and that the domain's identity and provenance types are constructible with those packages absent from the import graph. Assert that adapter-only vocabulary — table name, source family, source status, observed-field and normalized-field vectors, and county-native field names — appears nowhere in `property_tax_domain`.
<!-- countyforge-task: 4.5 paths=tests/architecture/test_dependency_direction.py checks=repo.check risk=higher_risk prerequisites=1.1,1.2,1.3,1.4,1.6 -->

- [ ] 4.6 Prove existing layers are untouched — Assert that the acquisition, Bronze, bounded-release-processing, county-adapter, and migration suites remain green and that this change alters none of their behavior. Confirm no PostgreSQL migration, S3 key, orchestration, or API surface changed.
<!-- countyforge-task: 4.6 paths=tests/architecture/test_dependency_direction.py checks=repo.check risk=normal prerequisites=4.1,4.2,4.3,4.4,4.5 -->
