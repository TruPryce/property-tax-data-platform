## 1. Approved Contracts and Independent Fixtures

<!-- countyforge-task: 1.1 paths=libs/property-tax-adapters/src/property_tax_adapters/sources/texas/tarrant.py,libs/property-tax-adapters/tests/fixtures/tarrant_synthetic.py checks=repo.check risk=normal prerequisites=D1,D2,D4 -->
- [ ] 1.1 Add the approved Tarrant physical contracts and independent fixtures — Extend the existing `tarrant.py` module with the parser contract version, the exact sixteen-name required header projection, the ISO-8859-1 and pipe/quote physical descriptor, the closed twenty-one-code diagnostic vocabulary required by D1, D2, and D4, and the two frozen interim types `TarrantValidationReport` and `TarrantDiagnostic` with exactly the fields the capability spec lists. Add `tarrant_synthetic.py` with small, independently authored, identity-free synthetic members covering valid rows, quoted delimiters, BOM and multiline rejection, header collisions, width mismatches, lexical boundary values, and duplicate accounts, each with literally authored expected results and no mirrored parser, county bytes, production rows, owner values, addresses, or runtime-generated expectations. Preserve `TARRANT_SOURCE` and the existing registry import surface. [source_ids: c197b56afee6f9e9f5fb3bb3, 7a428c5d41ca8b7be2132ac7, 7edd4e7de23f27539de02658]

## 2. Physical Parsing, Header Binding, and Fingerprint

<!-- countyforge-task: 2.1 paths=libs/property-tax-adapters/src/property_tax_adapters/sources/texas/tarrant.py checks=repo.check risk=higher_risk prerequisites=D1,D4,1.1 -->
- [ ] 2.1 Implement physical decoding, exact header binding, and the layout fingerprint — Implement strict ISO-8859-1 decoding with BOM rejection, pipe delimiting, doubled-quote handling, fail-closed malformed quoting, rejection of CR or LF inside quoted fields, LF and CRLF boundaries, one-based physical row numbering, untrimmed case-sensitive exact-name header binding, blank/duplicate/case-fold-collision/missing-header rejection, surrounding-whitespace header rejection as `unsupported_layout`, observed-width row validation, metadata-only extras recording `extra_columns_present`, and the layout fingerprint over the exact canonical JSON document specified in the capability spec. Retain the original ordered header vector separately in provenance and surface it through the bounded `TarrantValidationReport`. Add no archive acquisition, member selection, network access, or new dependency. [source_ids: c197b56afee6f9e9f5fb3bb3, 7edd4e7de23f27539de02658]

## 3. Lexical Validation, Uniqueness, and Release Atomicity

<!-- countyforge-task: 3.1 paths=libs/property-tax-adapters/src/property_tax_adapters/sources/texas/tarrant.py checks=repo.check risk=higher_risk prerequisites=D2,D4,D5,2.1 -->
- [ ] 3.1 Implement the approved lexical grammars, uniqueness, and atomic rejection — Implement ASCII-whitespace trimming, empty-text-only null handling, the `R`/`C`/`M`/`P` division rule, four-digit `Appraisal_Year` bounded 1900–2100 and matched against the caller-supplied expected source year, 1–64 visible-ASCII `Account_Num` preserved as text, 1–512 and 1–128 character optional identifier and text rules, the `[0-9]+(?:\.[0-9]{1,4})?` monetary grammar parsed with `decimal.Decimal` and bounded 0 through `10**28 - 1`, the D5 date grammar `^(0?[1-9]|1[0-2])/(0?[1-9]|[12][0-9]|3[01])/([0-9]{4})$` with calendar validity, the 1900-2100 range, and preserved lexical text, release-wide `Account_Num` uniqueness excluding division from the key, release-level atomic rejection returning zero records, and the 100-diagnostic retention cap with preserved total count and deterministic truncation. Reject any other separator, component order, or two-digit year with `invalid_source_date`, and do not normalize an accepted date, so `3/14/2025` and `03/14/2025` are both preserved as written. Enforce no arithmetic inequality, no padding, truncation, case folding, numeric coercion, silent rounding, or cross-field inference. This slice returns the bounded `TarrantValidationReport` specified in the capability spec — counts, diagnostics, fingerprint, and observed headers, with no row payload — and constructs no record type, so it imports nothing from `property_tax_adapters.sources.contracts`. [source_ids: 7a428c5d41ca8b7be2132ac7, 7edd4e7de23f27539de02658]

## 4. Privacy, Diagnostics, and Boundary Tests

<!-- countyforge-task: 4.1 paths=libs/property-tax-adapters/tests/test_tarrant_parser.py,libs/property-tax-adapters/tests/fixtures/tarrant_synthetic.py checks=repo.check risk=normal prerequisites=D1,D2,D4,1.1,2.1,3.1 -->
- [ ] 4.1 Add Tarrant parser, privacy, atomicity, and architecture tests — Cover valid single-line parsing, quoted delimiters and doubled quotes, BOM and multiline rejection, reordered headers with a metadata extra, blank/duplicate/case-fold-collision/missing/whitespace-padded headers, row-width mismatch, byte-exact fingerprint equality across reordered headers and its independence from layout validation, every lexical accept and reject boundary, empty-text-only nulls, unequal appraised and total acceptance, release-wide duplicate rejection and cross-release non-duplication, atomic zero-record rejection, the closed diagnostic vocabulary and its bounded metadata, deterministic 100-diagnostic truncation with preserved total, exclusion of sensitive and unknown-column values from records and diagnostics, date acceptance and rejection across separators, component orders, calendar-invalid values, and range bounds, and that `TarrantValidationReport` and `TarrantDiagnostic` expose exactly their specified fields and no row payload. For architecture, assert that `property_tax_domain` and `property_tax_application` are unchanged and that the parser adds no new vocabulary to them; the existing `TARRANT_SOURCE` registry definition legitimately imports both, so absence of those imports is not the assertion. Assert no network, acquisition, persistence, or new-dependency behavior. Ensure `make test` collects this exact module. [source_ids: 7edd4e7de23f27539de02658, a8d0164e015d086c00140812]

## 5. Bounded Source Documentation

<!-- countyforge-task: 5.1 paths=docs/sources/tarrant-parser-foundation.md checks=docs.links,repo.prepr-no-ai risk=low prerequisites=4.1 -->
- [ ] 5.1 Document the Tarrant certified-core parser foundation — Record the supported certified-core scope, the parser contract version, the required sixteen-header projection, the physical and lexical rules, the exact fingerprint document, the closed diagnostic vocabulary, the caller-supplied release identity, the privacy and unknown-column default-deny posture, and the synthetic fixture provenance. State explicitly that live-release compatibility, the complete 56-column header vector, the expected live fingerprint, source licensing, replacement behavior, and production readiness remain unproved; that the D5 date grammar is a synthetic-foundation choice rather than reproduced live evidence; that the bounded `TarrantValidationReport` is the complete interim public surface and is not a substitute for any shared contract; and that both the Tarrant-native record and the shared vendor-neutral conversion are deferred to Issue #43. [source_ids: 7edd4e7de23f27539de02658, 1b6acee37fd0a4979b5390da, 37b0be1db5c2ff2ef21f3eae]

## 6. Typed Records — Blocked on Issue #43

Both slices below construct or consume contracts owned by Issue #43 and are runnable only after those contracts are accepted and implemented. Tasks 1.1 through 5.1 do not depend on them.

<!-- countyforge-task: 6.1 paths=libs/property-tax-adapters/src/property_tax_adapters/sources/texas/tarrant.py,libs/property-tax-adapters/tests/test_tarrant_parser.py checks=repo.check risk=higher_risk prerequisites=D3,3.1,4.1 -->
- [ ] 6.1 Construct the frozen Tarrant-native record — **Blocked on Issue #43.** The approved record holds exact shared `SourceNativeValue` entries for the monetary and date fields, so it cannot be built before that contract exists and no county-local substitute may be written while waiting. Define the frozen `TarrantCertifiedSourceRecord` with `division_code`, `appraisal_year`, `account_num`, optional `pidn`, optional `gis_link`, optional `property_class`, optional `state_use_code`, optional `exemption_code`, shared `SourceNativeValue` entries, and `TarrantSourceProvenance` carrying jurisdiction `tx-tarrant`, release identifier, source member name, expected source year, source family, source status, the original ordered observed headers, exact-name and case-fold collision metadata, layout fingerprint, physical row number, parser contract version, and the exact source field and lexical text per source-native value. Populate no canonical appraisal or tax semantics. Add a separate row-materialization entry point that reuses task 3.1 validation rather than reimplementing it, and keep `TarrantValidationReport` valid and record-free. Add record tests to the existing Tarrant test module. [source_ids: 744640b7f3706d11325b9a7a, 7edd4e7de23f27539de02658]

<!-- countyforge-task: 6.2 paths=libs/property-tax-adapters/src/property_tax_adapters/sources/texas/tarrant.py,libs/property-tax-adapters/tests/test_tarrant_parser.py checks=repo.check risk=higher_risk prerequisites=D3,6.1 -->
- [ ] 6.2 Convert Tarrant native records through the shared Issue #43 contracts — **Blocked on Issue #43.** Import the shared `SourceNativeValue`, `SourceProvenance`, and `AppraisalSourceRecord` from `property_tax_adapters.sources.contracts` and convert each valid `TarrantCertifiedSourceRecord` into exactly one shared record using jurisdiction `tx-tarrant`, source account ID `Account_Num`, appraisal year `Appraisal_Year`, source family `certified-core`, source status `certified`, parcel reference `None`, source-native identifiers `Account_Num` plus nonblank `PIDN` and `GIS_Link`, source-native values keyed by exact Tarrant field name, and shared provenance derived from the Tarrant-native provenance. Populate no canonical market, appraised, assessed, taxable, tax-amount, payment, balance, delinquency, penalty, interest, exemption-entitlement, or replacement field, synthesize no current, exemption, companion, or replacement record, and create no county-local substitute. Add conversion tests to the existing Tarrant test module. [source_ids: 744640b7f3706d11325b9a7a, acd3ec5543a2bae6735f761b]
