## 1. Approved Contracts and Independent Fixtures

<!-- countyforge-task: 1.1 paths=libs/property-tax-adapters/src/property_tax_adapters/sources/texas/tarrant.py,libs/property-tax-adapters/tests/fixtures/tarrant_synthetic.py checks=repo.check risk=normal prerequisites=D1,D2,D3,D4 -->
- [ ] 1.1 Add the approved Tarrant physical contracts and independent fixtures — Extend the existing `tarrant.py` module with the parser contract version, the exact sixteen-name required header projection, the ISO-8859-1 and pipe/quote physical descriptor, and the closed twenty-one-code diagnostic vocabulary required by D1, D2, and D4. Add `tarrant_synthetic.py` with small, independently authored, identity-free synthetic members covering valid rows, quoted delimiters, BOM and multiline rejection, header collisions, width mismatches, lexical boundary values, and duplicate accounts, each with literally authored expected results and no mirrored parser, county bytes, production rows, owner values, addresses, or runtime-generated expectations. Preserve `TARRANT_SOURCE` and the existing registry import surface. [source_ids: c197b56afee6f9e9f5fb3bb3, 7a428c5d41ca8b7be2132ac7, 7edd4e7de23f27539de02658]

## 2. Physical Parsing, Header Binding, and Fingerprint

<!-- countyforge-task: 2.1 paths=libs/property-tax-adapters/src/property_tax_adapters/sources/texas/tarrant.py checks=repo.check risk=higher_risk prerequisites=D1,D4,1.1 -->
- [ ] 2.1 Implement physical decoding, exact header binding, and the layout fingerprint — Implement strict ISO-8859-1 decoding with BOM rejection, pipe delimiting, doubled-quote handling, fail-closed malformed quoting, rejection of CR or LF inside quoted fields, LF and CRLF boundaries, one-based physical row numbering, untrimmed case-sensitive exact-name header binding, blank/duplicate/case-fold-collision/missing-header rejection, observed-width row validation, metadata-only extras recording `extra_columns_present`, and the lowercase SHA-256 layout fingerprint over compact canonical JSON of contract version, encoding identifier, delimiter and quote behavior identifier, column count, and sorted observed header names. Retain the original ordered header vector separately in provenance. Add no archive acquisition, member selection, network access, or new dependency. [source_ids: c197b56afee6f9e9f5fb3bb3, 7edd4e7de23f27539de02658]

## 3. Lexical Validation and the Tarrant-Native Record

<!-- countyforge-task: 3.1 paths=libs/property-tax-adapters/src/property_tax_adapters/sources/texas/tarrant.py checks=repo.check risk=higher_risk prerequisites=D2,D3,D4,2.1 -->
- [ ] 3.1 Implement the approved lexical grammars, release atomicity, and the frozen native record — Implement ASCII-whitespace trimming, empty-text-only null handling, the `R`/`C`/`M`/`P` division rule, four-digit `Appraisal_Year` bounded 1900–2100 and matched against the caller-supplied expected source year, 1–64 visible-ASCII `Account_Num` preserved as text, 1–512 and 1–128 character optional identifier and text rules, the `[0-9]+(?:\.[0-9]{1,4})?` monetary grammar parsed with `decimal.Decimal` and bounded 0 through `10**28 - 1`, the calendar-valid date grammar bounded 1900–2100 with preserved lexical text, release-wide `Account_Num` uniqueness excluding division from the key, release-level atomic rejection returning zero records, and the 100-diagnostic retention cap with preserved total count and deterministic truncation. Define the frozen `TarrantCertifiedSourceRecord` and `TarrantSourceProvenance` carrying the approved fields and bounded provenance. Enforce no arithmetic inequality, no padding, truncation, case folding, numeric coercion, silent rounding, or cross-field inference. [source_ids: 7a428c5d41ca8b7be2132ac7, 744640b7f3706d11325b9a7a, 7edd4e7de23f27539de02658]

## 4. Privacy, Diagnostics, and Boundary Tests

<!-- countyforge-task: 4.1 paths=libs/property-tax-adapters/tests/test_tarrant_parser.py,libs/property-tax-adapters/tests/fixtures/tarrant_synthetic.py checks=repo.check risk=normal prerequisites=D1,D2,D4,1.1,2.1,3.1 -->
- [ ] 4.1 Add Tarrant parser, privacy, atomicity, and architecture tests — Cover valid single-line parsing, quoted delimiters and doubled quotes, BOM and multiline rejection, reordered headers with a metadata extra, blank/duplicate/case-fold-collision/missing headers, row-width mismatch, fingerprint equality across reordered headers and its independence from layout validation, every lexical accept and reject boundary, empty-text-only nulls, unequal appraised and total acceptance, release-wide duplicate rejection and cross-release non-duplication, atomic zero-record rejection, the closed diagnostic vocabulary and its bounded metadata, deterministic 100-diagnostic truncation with preserved total, exclusion of sensitive and unknown-column values from records and diagnostics, absence of domain and application imports, and absence of network, acquisition, persistence, and new-dependency behavior. Ensure `make test` collects this exact module. [source_ids: 7edd4e7de23f27539de02658, a8d0164e015d086c00140812]

## 5. Bounded Source Documentation

<!-- countyforge-task: 5.1 paths=docs/sources/tarrant-parser-foundation.md checks=docs.links,repo.prepr-no-ai risk=low prerequisites=4.1 -->
- [ ] 5.1 Document the Tarrant certified-core parser foundation — Record the supported certified-core scope, the parser contract version, the required sixteen-header projection, the physical and lexical rules, the closed diagnostic vocabulary, the caller-supplied release identity, the privacy and unknown-column default-deny posture, and the synthetic fixture provenance. State explicitly that live-release compatibility, the complete 56-column header vector, the expected live fingerprint, source licensing, replacement behavior, and production readiness remain unproved, and that shared vendor-neutral conversion is deferred to Issue #43. [source_ids: 7edd4e7de23f27539de02658, 1b6acee37fd0a4979b5390da, 37b0be1db5c2ff2ef21f3eae]

## 6. Shared Conversion — Blocked on Issue #43

<!-- countyforge-task: 6.1 paths=libs/property-tax-adapters/src/property_tax_adapters/sources/texas/tarrant.py,libs/property-tax-adapters/tests/test_tarrant_parser.py checks=repo.check risk=higher_risk prerequisites=D3,3.1,4.1 -->
- [ ] 6.1 Convert Tarrant native records through the shared Issue #43 contracts — **Blocked on Issue #43 and runnable only after those shared contracts are accepted and implemented; tasks 1.1 through 5.1 do not depend on it.** Import the shared `SourceNativeValue`, `SourceProvenance`, and `AppraisalSourceRecord` from `property_tax_adapters.sources.contracts` and convert each valid `TarrantCertifiedSourceRecord` into exactly one shared record using jurisdiction `tx-tarrant`, source account ID `Account_Num`, appraisal year `Appraisal_Year`, source family `certified-core`, source status `certified`, parcel reference `None`, source-native identifiers `Account_Num` plus nonblank `PIDN` and `GIS_Link`, source-native values keyed by exact Tarrant field name, and shared provenance derived from the Tarrant-native provenance. Populate no canonical market, appraised, assessed, taxable, tax-amount, payment, balance, delinquency, penalty, interest, exemption-entitlement, or replacement field, synthesize no current, exemption, companion, or replacement record, and create no county-local substitute. Add conversion tests to the existing Tarrant test module. [source_ids: 744640b7f3706d11325b9a7a, acd3ec5543a2bae6735f761b]
